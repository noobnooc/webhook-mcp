{
  "name": "webhook-mcp",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "agents": "^0.0.60",
    "hono": "^4.7.7"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}
